    - name: RESTART INFOBLOX SERVICE
      infoblox.nios_modules.nios_restartservices:
        services:
          - DHCP
        restart_option: RESTART_IF_NEEDED
        mode: GROUPED
        provider: "{{ nios_provider }}"
